# แผนคลิป C — Hardware Intro (แนะนำอุปกรณ์ Delta)

เขียนโดย Claude 2026-09-14 ตามที่ Min สั่ง ("ในวิดิโอนั้นวางแผนว่าจะนำเสนอยังไงแล้วบอกผมหน่อย")

---

## 0. ⛔ ความเข้าใจผิดที่ต้องแก้ก่อน (ต้นเหตุกล่อง "T" เต็มจอ)

**รูป `D:\Downloads\image.png`, `image2.png`, `image3.png`, `image4.png` คือ "เอกสารสเปก"**
— เป็นภาพหน้าจอตาราง/ไดอะแกรมที่บอกว่า *เราเลือกใช้รุ่นอะไร ทำหน้าที่อะไร ต่อกันยังไง*

**รูปพวกนี้ใช้เป็น "แหล่งข้อมูลสำหรับเขียนสคริปต์/ป้ายกำกับ" เท่านั้น ห้ามเอาไปแปะลงวิดีโอตรงๆ เด็ดขาด**
— ไม่ใช่ภาพถ่ายอุปกรณ์จริง ไม่ใช่ภาพโมเดล 3D ไม่ใช่ visual asset

**สิ่งที่เกิดขึ้นจริง (บั๊กที่ Min เห็น):** Gemini เข้าใจผิดว่าเป็นภาพที่ต้องเอาไปโชว์ เลยสร้าง
"reference-photo card" เป็นแผ่น 3D quad ไปแปะข้างอุปกรณ์ทุกตัวในฉาก Unity (งาน
`gt_mu01lt8iwl60z`) พอเท็กซ์เจอร์โหลดไม่ขึ้น เลยเรนเดอร์ออกมาเป็นกล่องสี่เหลี่ยม cyan มีตัว "T"
ลอยเต็มจอ — ต่อมามีงานสั่งลบทิ้ง (`gt_mu024xbk9mowm`) แต่ `C_HardwareIntro_FINAL.mp4` ถูกเรนเดอร์
ไปก่อนที่จะลบเสร็จ **ไฟล์นั้นจึงใช้ไม่ได้ ต้องเรนเดอร์ใหม่หลังลบการ์ดออกหมดแล้ว**

**ภาพถ่ายอุปกรณ์จริงที่ Min อยากได้ = คนละอย่างกับรูป image1-4** ต้องไปหาภาพสินค้าจริงของ Delta
แยกต่างหาก (ดู §4) แล้วเอาไปใส่ตอน **ตัดต่อ (post-production)** ไม่ใช่แปะเป็นวัตถุ 3D ในฉาก Unity

---

## 1. หน้าตาคลิปที่จะได้ (โครงรวม ~40-50 วิ)

| # | ช่วงเวลา | เนื้อหา | ทำที่ไหน |
|---|---|---|---|
| 0 | 0-5s | **เปิดเรื่อง** — กล้องกวาดตู้คอนโทรลทั้งตู้จากมุมกว้าง ขึ้นชื่อเรื่อง `DELTA AUTOMATION STACK` | Unity |
| 1 | 5-14s | **PLC AS320T-B** — pan เข้าหาตัว PLC ในตู้ + PLC เรืองแสงกระพริบ | Unity |
| 2 | 14-23s | **HMI DOP-100WS / 103WQ** — pan ไปที่จอ HMI บนหน้าตู้ + จอเรืองแสง | Unity |
| 3 | 23-32s | **VFD MS300** — pan ไปแถวล่างของตู้ + VFD เรืองแสง | Unity |
| 4 | 32-43s | **SERVO ASD-A3 ×2** — pan ไปที่ servo drive ในตู้ แล้ว**ตัดไปที่แกนจริง** (Guide Rail แกน X + Nozzle แกน Z) ให้เห็นว่า drive ตัวนี้คุมอะไรจริงๆ + ทั้ง drive และแกนเรืองแสงพร้อมกัน | Unity |
| 5 | 43-50s | **ปิดท้าย** — กล้องถอยออกเห็นทั้งระบบ ขึ้นข้อความสรุปว่าทั้ง 4 ตัวทำงานร่วมกันจริง | Unity |

> **จุดขายของช็อตที่ 4:** เป็นช็อตเดียวที่เชื่อม "กล่องในตู้" กับ "การเคลื่อนไหวจริงในไลน์" ให้กรรมการ
> เห็น ไม่ใช่แค่โชว์กล่องเปล่าๆ — ควรใช้เวลามากที่สุด

---

## 2. ป้ายข้อความบนจอ (ทุกตัวใช้ข้อมูลจาก image1-4 เป๊ะๆ ห้ามเดา)

รูปแบบเดียวกันทุกช็อต: **บรรทัดบน = ชื่อรุ่น (ตัวใหญ่)**, **บรรทัดล่าง = หน้าที่ (ตัวเล็ก)**

| ช็อต | บรรทัดบน | บรรทัดล่าง |
|---|---|---|
| 1 | `PLC — DELTA AS320T-B` | `Recipe Controller · 5-Parameter Table · High-Speed Counter` |
| 2 | `HMI — DELTA DOP-100WS / 103WQ` | `One-Touch Changeover · CIP Verification Checklist` |
| 3 | `VFD — DELTA MS300` | `Conveyor Speed Profile · S-Curve Anti-Tipping Ramp` |
| 4 | `SERVO — DELTA ASD-A3 (×2)` | `Axis X: Guide Rail Width · Axis Z: Nozzle Height & Dive Fill` |

**สไตล์ให้เหมือนคลิป A/B ที่ทำไปแล้ว** (ล่างซ้าย ขาว 30px กล่องดำโปร่ง 55%) จะได้ดูเป็นชุดเดียวกัน

---

## 3. เอฟเฟกต์เรืองแสง (ตามที่ Min ขอ "พูดถึงอันไหนให้อันนั้นเรืองแสง กระพริบๆ")

**มีของพร้อมใช้อยู่แล้ว ไม่ต้องเขียนใหม่:** `Assets\Scripts\MotionHighlightController.cs`
ทำ pulsing cyan emissive glow อยู่แล้ว (ใช้ `renderer.material` instanced + `_EmissionColor`
กระพริบด้วย `Mathf.Sin`) — **ให้ลอกแพทเทิร์นนี้** ไปทำตัวใหม่ เช่น `EquipmentIntroSequencer.cs`

ต่างกันแค่ *ตัวจุดชนวน*:
- ตัวเดิม (`MotionHighlightController`) ผูกกับ `ChangeoverSequencer.CurrentState` (S6/S7/S9)
- ตัวใหม่ต้องผูกกับ **ลำดับเวลาของคลิปเอง** (shot index) เพราะคลิปนี้ไม่มี state machine
  → โครงเหมือน `TimedShotSwitcher.cs` เป๊ะ: array ของ `(equipmentTransform, cameraTransform, holdDuration)`
  แล้ว `Update()` นับ `Time.deltaTime` สะสม เปลี่ยนช็อตเมื่อครบเวลา + เปิด glow บนอุปกรณ์ของช็อตนั้น

> ⚠️ **กฎที่ห้ามลืม (เคยพลาดมาแล้ว):** MonoBehaviour ตัวใหม่ที่ทำ glow ต้องถูก **ปิด** ระหว่าง
> เรนเดอร์คลิป A และ B ไม่งั้นมันจะไปเรืองแสงเบลดในคลิปอื่น — เพิ่มการปิด/เปิดใน
> `StartZoneRecordingNow()` และ `StartMulticamRecordingNow()` ใน `RecorderSmokeTest.cs`
> เหมือนที่ทำกับ `s_MotionHighlight` และ `s_Director.autoSwitchOnState` แล้ว

---

## 4. ภาพถ่ายอุปกรณ์จริง (ส่วนที่ Min อยากได้ "ขอบเล็กๆ โยงจากอุปกรณ์จริง")

**ทำตอนตัดต่อ ไม่ใช่ใน Unity** — Min สั่งว่า "พอทำวิดิโอใน unity แล้วก็บอก gemini ว่าเอาไปตัดกับ
hyperframe แล้ว insert ภาพแต่ละรุ่น model เป็นขอบเล็กๆ โยงจากอุปกรณ์จริง"

**แปลว่า:** ตอนอุปกรณ์ตัวไหนเรืองแสงอยู่ในเฟรม ให้มี **ภาพถ่ายสินค้าจริงของรุ่นนั้น** โผล่มาเป็น
**กรอบเล็กๆ ที่มุมจอ** พร้อม**เส้นโยง**ลากจากกรอบนั้นไปยังตัวอุปกรณ์ 3D ในฉาก (แบบ leader line)

### 4.1 ต้องไปหาภาพถ่ายจริงมาก่อน (ยังไม่มีในเครื่อง)

ต้องได้ภาพสินค้าจริง 4 รุ่นนี้ (พื้นหลังขาว ภาพทางการจาก Delta จะดีที่สุด):

| รุ่น | หาได้ที่ |
|---|---|
| PLC AS320T-B | https://deltaacdrives.com/delta-as320t-b-as-series-plc/ · https://www.deltaww.com/en-US/products/PLC-Programmable-Logic-Controllers/3495 · `Delta-AS-Series-Catalog.pdf` |
| HMI DOP-100WS / 103WQ | https://industrialautomation.delta-emea.com (หมวด HMI DOP-100 series) |
| VFD MS300 | https://deltaacdrives.com/delta-vfd-ms300-micro-drive/ · https://www.e-motionsupply.com/Delta_VFD_MS300_s/4307.htm |
| SERVO ASD-A3 | https://www.deltaww.com (หมวด Servo Systems ASDA-A3) |

**ข้อควรระวัง:** ใช้ภาพทางการของ Delta เอง (เป็นการแข่งที่ Delta สนับสนุน การอ้างอิงรุ่นสินค้าจริง
พร้อมภาพทางการถือเป็นเรื่องปกติ) — เก็บไฟล์ไว้ที่ `D:\unity_project\delta_academy\Assets\ReferenceImages\`
(โฟลเดอร์นี้มีอยู่แล้ว) ตั้งชื่อไฟล์ให้ตรงรุ่น เช่น `AS320T-B.png`, `DOP-100WS.png`, `MS300.png`, `ASD-A3.png`

### 4.2 หน้าตาที่ต้องการตอนตัดต่อ

```
┌─────────────────────────────────────────────┐
│                                             │
│                                   ┌───────┐ │
│        [อุปกรณ์ 3D เรืองแสง] ─────│ รูป   │ │  ← กรอบเล็กมุมขวาบน
│                  ▲                │ จริง  │ │     + เส้นโยงมาที่อุปกรณ์
│                  └── เส้นโยง      └───────┘ │
│                                             │
│  ┌──────────────────────────────┐           │
│  │ PLC — DELTA AS320T-B         │           │  ← ป้ายล่างซ้าย (§2)
│  │ Recipe Controller · 5-Param  │           │
│  └──────────────────────────────┘           │
└─────────────────────────────────────────────┘
```

- กรอบรูปจริง: มุมขวาบน ขนาดราว 20-25% ของความกว้างจอ มีขอบบางสีขาว/cyan ให้ดูเป็น callout
- เส้นโยง: จากขอบกรอบลากไปหาตำแหน่งอุปกรณ์ในฉาก (ตำแหน่งคงที่ได้ เพราะกล้องแต่ละช็อตล็อกมุม)
- โผล่พร้อมกับตอนอุปกรณ์เริ่มเรืองแสง หายไปพร้อมกันตอนตัดช็อต

---

## 5. ลำดับงาน (ใครทำอะไร)

| ลำดับ | งาน | คนทำ |
|---|---|---|
| 1 | **ลบ reference-photo card ทิ้งให้หมดก่อน** (ต้นเหตุกล่อง "T") แล้วยืนยันด้วยการสกัดเฟรมว่าไม่เหลือแล้วจริง | Unity agent |
| 2 | ปรับโมเดล 3D ของ PLC/HMI/VFD/Servo ในตู้ให้ดูสมจริงขึ้น (ตอนนี้เป็น blockout กล่องเปล่าๆ) | Unity agent |
| 3 | ทำ `EquipmentIntroSequencer.cs` (กล้อง pan + glow ตามลำดับช็อต §1, §3) | Unity agent |
| 4 | เรนเดอร์คลิปดิบ `C_HardwareIntro_RAW_*.mp4` แล้ว**สกัดเฟรมตรวจทุกช็อต**ก่อนส่งต่อ | Unity agent |
| 5 | หาภาพถ่ายจริง 4 รุ่น (§4.1) เก็บใน `Assets\ReferenceImages\` | ใครก็ได้ |
| 6 | ตัดต่อด้วย hyperframe: ใส่ป้ายข้อความ (§2) + กรอบรูปจริงมุมขวาบนพร้อมเส้นโยง (§4.2) | Gemini |
| 7 | ตรวจเฟรมจริงรอบสุดท้าย แล้วแนบเข้ากระดานให้ Min ดู | ใครก็ได้ |

---

## 6. กฎตรวจงาน (เหมือนคลิป A/B)

`ffprobe` ผ่าน **ไม่ได้แปลว่าเนื้อหาถูก** — บั๊กกล่อง "T" นี่คือตัวอย่างชัดที่สุด ไฟล์ valid สมบูรณ์
แต่เนื้อภาพใช้ไม่ได้เลย **ต้องสกัดเฟรมออกมาดูด้วยตาทุกช็อต** (อย่างน้อยช็อตละ 1 เฟรม รวม 6 เฟรม
ตาม §1) แล้วเช็ค: ชื่อรุ่นสะกดถูกไหม / อุปกรณ์ที่เรืองแสงตรงกับป้ายไหม / ไม่มีวัตถุแปลกปลอมค้างในฉาก
