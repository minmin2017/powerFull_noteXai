# PLAN.md — Delta Academy Video Project (แผนรวม ล่าสุด)

> อัปเดตล่าสุด: 2026-09-14 ~18:45 โดย Claude
> **ไฟล์นี้คือแหล่งความจริงหลัก** — ถ้าลืมอะไร ให้กลับมาอ่านไฟล์นี้ก่อน
> ไฟล์ประกอบ: `CLIP_C_HARDWARE_INTRO_PLAN.md` (รายละเอียดคลิป C), `CODEX_BRIEF_DELTA_ACADEMY.md`
> (บริบท+กับดักทั้งหมด), `HANDOFF.md` (ประวัติงาน)

---

## 0. ภาพรวม — มี 3 คลิป

| คลิป | ชื่อ | ความยาว | สถานะ |
|---|---|---|---|
| **C** | Hardware Intro + System Architecture | ~50-60s | ⬜ ยังไม่เสร็จ (มีดราฟต์ที่พังอยู่ ต้องทำใหม่) |
| **A** | Line Overview (พาชมทั้งไลน์) | 90.57s | ✅ **เสร็จแล้ว** `A_LineOverview_FINAL_v2.mp4` |
| **B** | Changeover (กลไกเปลี่ยนรุ่น) | 45.03s → จะขยายเป็น ~90s | 🔶 มีตัวใช้ได้แล้ว แต่**ต้องเพิ่มขั้นตอนการนำเสนอ** (§2) |

**ลำดับการฉาย: C → A → B** (C เป็นคลิปแรก อธิบายว่ามีอุปกรณ์อะไรบ้าง ก่อนไปดูไลน์จริง)

> ⛔ **ขอบเขตของ Min = อธิบายกลไกเท่านั้น** ห้ามใส่ ROI/ต้นทุน/ตัวเลขเงิน (เป็นงานเพื่อนร่วมทีม)

---

## 1. 🆕 คลิป C — Hardware Intro (คลิปแรก) — 2 พาร์ท

### พาร์ท 1: System Architecture (ตัดด้วย hyperframe ทีหลัง — ไม่ใช่ Unity)

โชว์แผนผังว่าอุปกรณ์ 4 ชนิดต่อกันยังไง **ให้เห็นภาพรวมคร่าวๆ ก่อน แล้วค่อยลงลึกในพาร์ท 2**

Min ส่งภาพตัวอย่างเลย์เอาต์มาให้ (**เป็นแค่ตัวอย่างหน้าตา ไม่ใช่ข้อมูลจริง** — ข้อมูลจริงใช้จาก
`D:\Downloads\image.png`–`image4.png`) โครงที่ต้องการ:

```
              ┌──────────────────────────────────────┐
              │ DIACloud — Changeover Log/Dashboard  │   (บนสุด, teal)
              └──────────────────┬───────────────────┘
                                 ┆ (เส้นประ)
┌──────────────────┐   Modbus TCP │            ┌─────────────────────┐
│ HMI DOP-100WS    │─────────────►│            │ Sensors/Limit Switch│
│ เลือก Recipe     │     ┌────────┴─────────┐  │ Home sensor,        │
│ แสดงสถานะ/จับเวลา│     │ PLC AS320T-B     │◄─│ ตรวจจับชิ้นงาน       │
└──────────────────┘     │ Recipe Table +   │  └─────────────────────┘
                         │ Sequence Control │
                         │ Interlock/Safety │
                         └────────┬─────────┘
                 Modbus RTU / Pulse Train
         ┌───────────────┬────────┴────────┬──────────────────┐
  ┌──────▼──────┐ ┌──────▼──────┐   ┌──────▼──────┐
  │SERVO ASD-A3 │ │SERVO ASD-A3 │   │ VFD MS300   │
  │แกน X ปรับ   │ │แกน Z ปรับ   │   │ speed       │
  │กว้าง rail   │ │สูงหัวจ่าย   │   │ profile     │
  └─────────────┘ └─────────────┘   └─────────────┘
```

**ต้องทำให้เห็นภาพชัดกว่าภาพตัวอย่าง** — แนะนำให้ทยอยโผล่ทีละกล่อง (PLC ก่อน แล้วค่อยแตกออกไป
หา HMI/Servo/VFD) พร้อมเส้นเชื่อมที่วิ่งมีทิศทาง ให้เห็นว่าใครสั่งใคร

### พาร์ท 2: Unity 3D deep dive

6 ช็อต ~40-50s — **รายละเอียดเต็มอยู่ที่ `CLIP_C_HARDWARE_INTRO_PLAN.md`** สรุปสั้น:

| ช็อต | เนื้อหา |
|---|---|
| 0 (0-5s) | กวาดตู้คอนโทรลมุมกว้าง + หัวเรื่อง |
| 1 (5-14s) | PLC AS320T-B — pan เข้า + เรืองแสงกระพริบ |
| 2 (14-23s) | HMI DOP-100WS/103WQ |
| 3 (23-32s) | VFD MS300 |
| 4 (32-43s) | SERVO ASD-A3 ×2 — **ตัดไปโชว์แกนจริงด้วย** (ราง X + หัวฉีด Z) ช็อตนี้สำคัญสุด |
| 5 (43-50s) | ถอยกล้องเห็นทั้งระบบ ปิดท้าย |

### ⛔ บั๊กที่ต้องแก้ก่อนทำต่อ

คลิปเดิม `C_HardwareIntro_FINAL.mp4` **ใช้ไม่ได้** — มีกล่องสี่เหลี่ยม cyan ตัว "T" ลอยทับเต็มจอ
(Min: "ไม่ professional เลย") สาเหตุ: Gemini เข้าใจผิดว่ารูป image1-4 คือภาพที่ต้องโชว์ เลยสร้าง
"reference-photo card" เป็นแผ่น 3D ไปแปะข้างอุปกรณ์ทุกตัว แล้วเท็กซ์เจอร์โหลดไม่ขึ้น
→ **ต้องลบการ์ดทิ้งให้หมด แล้วเรนเดอร์ใหม่ ยืนยันด้วยการสกัดเฟรมจริง**

---

## 2. 🆕 คลิป B — เพิ่มขั้นตอนการนำเสนอ (45s → ~90s)

Min: *"จากภาพจะเห็นว่ามีหลายขั้นตอน การนำเสนอแบบนี้จะให้กรรมการเห็นภาพมากขึ้น"*

**นี่คือคำตอบของคำถามค้างเรื่องความยาว** — แทนที่จะยืดด้วย slow-motion (ซึ่งทำลายสารว่า
"เปลี่ยนรุ่นได้เร็ว") ให้**เพิ่มขั้นตอนจริงเข้าไป** ได้ทั้งความยาวและความเข้าใจ

| ขั้น | เนื้อหา | ทำที่ไหน |
|---|---|---|
| **1** | **Flow overview** — บอกภาพรวมก่อนว่าทั้งกระบวนการมีขั้นตอนอะไรบ้าง (คล้ายพาร์ท 1 ของคลิป C) | **hyperframe (ตัดทีหลัง)** |
| **2** | **เลือก Recipe** — หน้าจอ HMI เหมือนจริง กดเลือกขนาดขวด **ถ้ามีภาพประกอบว่าแต่ละ recipe หน้าตายังไง (ขนาดขวด) จะดีมาก** | Unity (ต่อยอดจาก HMI ที่ Gemini เคยทำ) |
| **3** | **Loading parameter** — animation ตอนระบบโหลดค่า 5 พารามิเตอร์เข้าไป | Unity |
| **4** | **Auto positioning** — แสดงว่าระบบปรับตัวยังไง ปรับอะไรบ้าง เริ่มจาก **Servo homing to home** | Unity |
| **5** | **PLC สั่งงาน** — animation ว่า PLC สั่ง VFD → VFD ปรับความเร็วสายพาน, PLC สั่ง Servo → เลื่อนตำแหน่งราง/ความสูงหัวจ่าย | Unity |
| **6** | **Verify and start** — ตรวจยืนยันแล้วเริ่มผลิต | Unity |

**ของที่มีอยู่แล้วใช้ต่อได้:**
- `ChangeoverSequencer.cs` มี state S1-S10 ครบ (ขั้น 4-6 แมปกับ S6/S7/S8/S9/S10 ได้เลย)
- `MotionHighlightController.cs` ทำ glow + ตัวเลข mm สดแล้ว
- `DeltaHMIDisplay.cs` มีหน้าจอ HMI แสดง recipe/checklist อยู่แล้ว (ขั้น 2 ต่อยอดจากตัวนี้)

---

## 3. รูปถ่ายอุปกรณ์จริง (ใช้ทั้งคลิป C และ B ขั้น 2)

**สถานะ: ✅ มีครบทั้ง 5 รูปแล้ว** (โหลดไว้ตั้งแต่ 2026-09-13 19:25 — ตรวจแล้วเป็นภาพทางการ Delta
พื้นหลังขาว คุณภาพดี ใช้ได้เลย) เก็บที่ `D:\unity_project\delta_academy\Assets\ReferenceImages\`

| รุ่น | ไฟล์ | ขนาด |
|---|---|---|
| PLC AS320T-B | ✅ `AS320T-B_front.jpg` | 22 KB |
| HMI DOP-100WS | ✅ `DOP-100WS_front.jpg` | 111 KB |
| VFD MS300 | ✅ `MS300_front.jpg` | 104 KB |
| SERVO ASD-A3 | ✅ `ASD-A3_front.jpg` | 170 KB |
| PSU CliQ-M | ✅ `CliQ-M_front.jpg` | 1.2 MB |

> ⚠️ **แปลว่าบั๊กกล่อง "T" ไม่ได้เกิดจาก "ไม่มีรูป"** — รูปมีครบและใช้ได้ปกติ ปัญหาอยู่ที่
> **วิธีเอารูปไปแปะบนแผ่นการ์ด 3D** (material/shader ตั้งไม่ถูก หรือ texture import setting ผิด
> หรือ material reference หลุด) → ยิ่งยืนยันว่าไม่ควรทำเป็นแผ่น 3D ในฉากตั้งแต่แรก ให้ใส่ตอนตัดต่อแทน

**วิธีใช้:** ใส่ตอน**ตัดต่อ** เป็น**กรอบเล็กมุมขวาบน + เส้นโยง**ไปหาอุปกรณ์ 3D ในฉาก
(**ห้ามทำเป็นแผ่น 3D ในฉาก Unity** — นั่นคือสาเหตุบั๊กกล่อง "T")

---

## 4. ⛔ กฎเหล็ก (ที่เคยพลาดมาแล้วทั้งนั้น)

1. **รูป `D:\Downloads\image.png`–`image4.png` = เอกสารสเปก ใช้เป็นข้อมูลเขียนป้าย/สคริปต์เท่านั้น
   ห้ามแปะลงวิดีโอ ห้ามทำเป็นวัตถุ 3D**
2. **`ffprobe` ผ่าน ≠ เนื้อหาถูก** — ต้องสกัดเฟรมดูด้วยตาทุกครั้ง (บั๊กกล่อง "T" คือตัวอย่างชัดสุด:
   ไฟล์สมบูรณ์ แต่ภาพใช้ไม่ได้เลย)
3. **Save scene ทุกครั้งหลังรัน builder** ก่อนเข้า Play Mode ไม่งั้นงานหายเงียบๆ
4. **Overlay ที่ต้องติดในคลิปต้องเป็น world-space TextMeshPro เท่านั้น** — Screen Space Overlay
   canvas ไม่ถูกจับโดย Recorder
5. **MonoBehaviour ที่ทำ glow ต้องปิดตอนเรนเดอร์คลิปอื่น** ไม่งั้นเบลดข้ามคลิป
6. **ห้ามทิ้ง component ทดสอบ enabled ค้างบน GameObject "Cell"** — เคยมี
   `HardwareIntroManualRecorder` ค้างแล้ว force-quit Play Mode ตัดคลิปที่ agent อื่นเรนเดอร์อยู่ให้สั้น
7. **ชื่อรุ่นต้องตรงเป๊ะ**: AS320T-B / DOP-100WS / 103WQ / MS300 / ASD-A3
8. **Codex กับ Gemini ทำงานใน Unity Editor เดียวกัน** — ระวังชนกัน

---

## 5. ไฟล์ผลลัพธ์ปัจจุบัน

| ไฟล์ | ยาว | สถานะ |
|---|---|---|
| `Recordings\A_LineOverview_FINAL_v2.mp4` | 90.57s | ✅ ใช้ได้ ห้ามแก้ |
| `Recordings\B_Changeover_FINAL_v4.mp4` | 45.03s | ✅ ใช้ได้ (แต่จะถูกแทนด้วยเวอร์ชันขยายตาม §2) |
| `Recordings\C_HardwareIntro_FINAL.mp4` | 32s | ❌ พัง (กล่อง "T") ต้องทำใหม่ |

---

## 6. ลำดับงานถัดไป

1. ⬜ ลบ reference-photo card (กล่อง T) ออกจากฉากให้หมด + ยืนยันด้วยเฟรมจริง
2. ⬜ โหลดรูปสินค้าจริงที่เหลือ 3 รุ่น (§3)
3. ⬜ ปรับโมเดล 3D ของ PLC/HMI/VFD/Servo ให้สมจริงขึ้น (ตอนนี้เป็นกล่อง blockout)
4. ⬜ ทำ `EquipmentIntroSequencer.cs` (pan + glow ตามลำดับช็อต) → เรนเดอร์คลิป C พาร์ท 2
5. ⬜ ขยายคลิป B ตามขั้นตอน §2 (ขั้น 2-6 ใน Unity)
6. ⬜ ส่งให้ Gemini ตัด hyperframe: architecture diagram (คลิป C พาร์ท 1), flow overview
   (คลิป B ขั้น 1), กรอบรูปจริง + เส้นโยง
7. ⬜ ตรวจเฟรมจริงทุกคลิป → แนบเข้ากระดานให้ Min ดู
