# สเปก — System Architecture Diagram (คลิป C พาร์ท 1, ~15-18s)

เขียนโดย Claude 2026-09-14 · ใช้คู่กับ `PLAN.md` §1 และ `CLIP_C_HARDWARE_INTRO_PLAN.md`

**เป้าหมาย:** เปิดคลิป C ด้วยแผนผังที่ทำให้กรรมการเข้าใจใน 15 วินาทีว่า *อุปกรณ์ Delta 4 ชนิด
คุยกันยังไง ใครสั่งใคร* ก่อนจะตัดเข้าไปดูของจริงใน Unity 3D

> Min ส่งภาพตัวอย่างเลย์เอาต์มาให้แล้ว แต่บอกชัดว่า **"อันนี้ภาพตัวอย่างไม่ใช่ข้อมูลจริง
> ทำให้เห็นภาพมากขึ้นนะ"** → ห้ามเอาภาพนั้นไปแปะตรงๆ ต้องสร้างใหม่ให้ดีกว่าเดิม

---

## 1. หลักคิด — ทำไมต้อง "ทยอยโผล่" ไม่ใช่โชว์ทั้งแผนผังทีเดียว

แผนผังนิ่งที่มีกล่อง 7 กล่องโผล่พร้อมกัน = กรรมการต้องกวาดตาหาเองว่าอะไรสำคัญ **ลำดับการโผล่
คือการเล่าเรื่อง** — ให้โผล่ตามลำดับที่สมองเข้าใจง่ายที่สุด:

```
1. PLC ก่อน (สมองของระบบ)        → "ทุกอย่างวิ่งผ่านตัวนี้"
2. HMI (คนสั่งงาน)                → "คนกดสั่งตรงนี้"
3. Servo X, Servo Z, VFD (มือและเท้า) → "PLC สั่งพวกนี้ไปขยับของจริง"
4. Sensors (ประสาทสัมผัส)         → "แล้วส่งค่ากลับมายืนยัน"
5. DIACloud (ความจำ)              → "ทุกครั้งที่เปลี่ยนรุ่น ถูกบันทึกไว้หมด"
```

เล่าแบบนี้ = สมอง/มือ/ประสาทสัมผัส/ความจำ ซึ่งเป็นอุปมาที่คนเข้าใจทันทีโดยไม่ต้องอธิบาย

---

## 2. ไทม์ไลน์ทีละวินาที

| เวลา | สิ่งที่เกิดขึ้น | ข้อความที่ขึ้น |
|---|---|---|
| 0.0-1.5s | จอว่าง → กล่อง **PLC** fade in ตรงกลาง (กล่องใหญ่สุด สีเข้มสุด) | `PLC — DELTA AS320T-B` / `Recipe Table · Sequence Control · Safety Interlock` |
| 1.5-3.5s | กล่อง **HMI** โผล่ทางซ้าย + เส้นเชื่อมวิ่งจาก HMI → PLC | `HMI — DOP-100WS` / `เลือก Recipe · จับเวลา Changeover` · ป้ายบนเส้น: `Modbus TCP` |
| 3.5-7.5s | กล่อง **Servo X / Servo Z / VFD** โผล่ทีละตัวข้างล่าง (ห่างกัน ~1.3s) + เส้นวิ่งจาก PLC ลงไปหาแต่ละตัว | `SERVO ASD-A3 — แกน X` / `ปรับความกว้าง Guide Rail`<br>`SERVO ASD-A3 — แกน Z` / `ปรับความสูงหัวจ่าย · Dive Filling`<br>`VFD MS300` / `ปรับความเร็วสายพาน` · ป้ายบนเส้น: `Pulse Train / PR Mode` (servo), `Modbus RTU` (VFD) |
| 7.5-9.5s | กล่อง **Sensors** โผล่ทางขวา + เส้นวิ่ง**ย้อนกลับ** เข้าหา PLC (คนละสีกับเส้นสั่งงาน) | `SENSORS / LIMIT SWITCH` / `Home Sensor · ยืนยันตำแหน่งก่อนเริ่มผลิต` |
| 9.5-11.5s | กล่อง **DIACloud** โผล่บนสุด + เส้นประขึ้นจาก PLC | `DIACloud` / `Changeover Log · Remote Dashboard` |
| 11.5-15.0s | แผนผังครบทั้งหมดค้างไว้ → pulse วิ่งรอบเส้นทั้งหมด 1 รอบ (ให้เห็นว่าระบบมีชีวิต) → fade out เตรียมตัดเข้า Unity | ข้อความสรุปล่างจอ: `4 DEVICES · ONE RECIPE · UNDER 5 MINUTES` |

**ความยาวรวม ~15s** (ยืดได้ถึง 18s ถ้าจังหวะแน่นไป)

---

## 3. กฎการออกแบบภาพ

1. **เส้นต้องมีทิศทาง** — ใช้ pulse/แสงวิ่งไปตามเส้น ไม่ใช่เส้นนิ่งๆ มีหัวลูกศร
   เพราะ "ใครสั่งใคร" คือสาระหลักของแผนผังนี้
2. **แยกสีเส้นสั่งงาน vs เส้นป้อนกลับ**
   - เส้นสั่งงาน (PLC → Servo/VFD): สีสว่าง เช่น cyan/ฟ้า วิ่งออกจาก PLC
   - เส้นป้อนกลับ (Sensors → PLC): สีต่างออกไป เช่น เขียว/ส้ม วิ่งเข้าหา PLC
   - เส้น DIACloud: เส้นประ (บอกว่าเป็น cloud ไม่ใช่สายจริง)
3. **PLC ต้องเด่นที่สุดตลอดเวลา** — ใหญ่กว่า เข้มกว่า อยู่กลาง เพราะทุกเส้นวิ่งผ่านมัน
4. **ป้ายโปรโตคอลบนเส้น** (`Modbus TCP` / `Modbus RTU` / `Pulse Train`) — อย่าละทิ้ง
   นี่คือรายละเอียดวิศวกรรมจริงที่ทำให้กรรมการเห็นว่าเข้าใจงานจริง ไม่ใช่วาดกล่องสวยๆ
5. **ตอนจบต้องเชื่อมกับช็อตถัดไป** — เฟรมสุดท้ายควร fade ไปทางมุมกล้องกว้างของตู้คอนโทรลใน
   Unity (ช็อต 0 ของพาร์ท 2) ให้รู้สึกว่า "แผนผังนี้ = ของจริงที่กำลังจะเห็น"

---

## 4. ข้อมูลที่ต้องใช้ (ห้ามเดา)

ชื่อรุ่น/หน้าที่/โปรโตคอลทั้งหมด **มาจาก `PLAN.md` §1 และ `CODEX_BRIEF_DELTA_ACADEMY.md` §8**
(ซึ่ง transcribe มาจากรูป `D:\Downloads\image.png`–`image4.png` แล้ว)

| อุปกรณ์ | รุ่นเป๊ะๆ | สื่อสารด้วย |
|---|---|---|
| PLC | AS320T-B | — (ศูนย์กลาง) |
| HMI | DOP-100WS / 103WQ | Modbus TCP ↔ PLC |
| Servo แกน X | ASD-A3 | Pulse Train / PR Mode ← PLC |
| Servo แกน Z | ASD-A3 | Pulse Train / PR Mode ← PLC |
| VFD | MS300 | Modbus RTU ← PLC |
| Sensors | Home sensor / Limit switch | → PLC (ป้อนกลับ) |
| Cloud | DIACloud | Ethernet (เส้นประ) ↔ PLC |

> ⛔ **ห้ามสะกดรุ่นผิดเด็ดขาด** — เป็นการแข่งที่ Delta เป็นเจ้าภาพ กรรมการรู้จักรุ่นพวกนี้ดีกว่าเรา

---

## 5. เรื่องเทคนิค — สร้างยังไงก็ได้ ขอให้ผลลัพธ์ตรงสเปก

Min บอกให้ตัดด้วย **hyperframe** — วิธีสร้างแผนผังปล่อยให้ผู้ทำเลือกตามถนัด เช่น
- ทำเป็น motion graphics ใน hyperframe ตรงๆ
- หรือสร้างเป็นหน้า HTML/SVG แล้ว screen-record / export เป็นเฟรม แล้วเอาไปต่อ
- หรือ generate เฟรมด้วยสคริปต์ (Python/PIL, ffmpeg overlay ทีละช่วง)

**ข้อกำหนดผลลัพธ์:** 1280×720, 30fps, h264 — ให้ตรงกับคลิป A/B/C ที่มีอยู่ จะได้ concat ได้โดย
ไม่ต้อง re-encode

**ตรวจก่อนส่ง:** สกัดเฟรมจริงที่ t=1, 3, 5, 7, 9, 11, 14 ดูว่ากล่องโผล่ครบตามไทม์ไลน์ §2 จริงไหม
ข้อความอ่านออกไหม ชื่อรุ่นสะกดถูกไหม ไม่ล้นจอไหม
