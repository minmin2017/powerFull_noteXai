name: Render Manim Videos (Docker)

on:
  workflow_dispatch:
    inputs:
      scene_file:
        description: 'Path to python script'
        required: true
        default: 'valves_circuits_anim.py'
      scene_names:
        description: 'Scenes to render (space-separated)'
        required: true
        default: 'HV01_CheckValve HV02_SpoolValve HC01_Regenerative'

jobs:
  render:
    runs-on: ubuntu-latest
    container: manimcommunity/manim:latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Install Thai Font & Language Support (5 seconds in Docker)
        run: |
          sudo apt-get update && sudo apt-get install -y fonts-thai-tlwg texlive-lang-other

      - name: Render Manim Video
        run: |
          manim -qm --fps 30 ${{ github.event.inputs.scene_file }} ${{ github.event.inputs.scene_names }}

      - name: Upload Video Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: rendered-manim-videos
          path: media/videos/
